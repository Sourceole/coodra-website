## 2026-04-25 - Landing cleanup, QA, deploy, and continuity log

### 1) Landing page cleanup (safe passes)
- Performed targeted cleanup focused on removing legacy bloat without changing current design intent.
- Scope centered on:
  - `src/pages/LandingPage.tsx`
  - `src/pages/LandingPage.css`
  - `public/landing/app.js`
  - Marketing shell files (`MarketingHeader*`, `MarketingFooter*`, `MarketingMedia*`).
- Removed/trimmed old systems only where usage was confirmed dead.
- Preserved active systems and behavior-critical selectors used by current markup and scripts.

### 2) QA methodology and outcome
- Ran final verification in **preview mode** (not dev HMR) to avoid false hydration/HMR errors.
- Commands:
  - `npm run build` -> pass
  - `npx eslint src/pages/LandingPage.tsx` -> pass
- Visual QA viewports:
  - 1440x900
  - 1280x800
  - 768x1024
  - 390x844
  - mobile menu open state (390 emulated mobile/touch)
- Checks performed:
  - header/menu behavior and mobile open/close
  - hero/trust/how/ai-chat/workflow/impact/cta/footer layout
  - reveal behavior
  - hover interactions
  - no horizontal overflow
  - no broken links/images on landing
  - console/runtime health
- QA verdict: safe to deploy.

### 3) Deployment
- Deployed to Vercel production with alias to `https://www.coodra.com`.
- Deployment reference:
  - `dpl_5YLBhnGYGnTeqbZmqvqcVKVvENZn`
  - Inspector: `https://vercel.com/mikes-projects-405e3d20/coodra-website/5YLBhnGYGnTeqbZmqvqcVKVvENZn`

### 4) Dashboard/integrations continuity notes
- Integration connect logic is scaffolded end-to-end (start + callback + state handling), but real provider credentials/config are still needed for all providers to fully complete OAuth.
- Shopify behavior note:
  - If test user does not have Shopify account/session, Shopify OAuth will route through account access/auth screens as expected.
- Performance center empty state and nav spacing/full-sync placement have been iterated repeatedly; keep future edits minimal and replacement-based (avoid override layering).

### 5) Auth / chat / MFA continuity notes
- MFA template has been iterated for branding and sizing; code display formatting was adjusted to remove split grouping when requested.
- Dashboard chat stream behavior improved:
  - startup delay reduced
  - thinking stream made more natural than fixed repeated lines.

### 6) Cross-repo dependency note
- Some backend enforcement work and SQL audit schema updates are in `sourceole-ai-backend` (outside this repo).
- Keep cross-repo changes coordinated when validating claims on `www.coodra.com` vs API behaviors on `api.coodra.com`.

