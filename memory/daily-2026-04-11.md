# Daily Memory - 2026-04-11

## Work Completed
- Added mobile hamburger navigation behavior on landing page:
  - `src/pages/LandingPage.tsx`
  - `src/pages/LandingPage.css`
- Fixed hamburger icon orientation to classic horizontal 3-line style.
- Fixed landing section rendering issues:
  - "Three steps from signal to action" now properly shows all 3 steps on mobile/fallback.
  - "Decisions that are fast, clear, and measurable" now shows A/B/C cards (not just C).
- Added hero background interactive grid hover behavior (per-cell highlight effect).
- Fixed production redirect loop by removing recursive redirects in `vercel.json`.
- Retailer UX microcopy polish in command center script:
  - replaced "coming soon"-style copy with production wording.
  - files: `public/wh-command-center.web.js`, `src/pages/Dashboard.tsx` (asset version bump).

## Key Deploys Today
- Landing/mobile/layout fixes and nav updates deployed to `www.coodra.com`.
- Redirect fix deployed and verified no redirect loop.

## Context Received
- Planned migration to React Router v7 framework SSR:
  - move routes to `src/routes/`
  - remove manual `src/main.tsx` routing
  - add RRv7 SSR-aware Vite config
  - preserve existing auth/component logic and CSS

## Next Session Priority
- Continue polish pass after SSR migration branch is ready.

## CSP / SSR Fix
- Fixed landing CSP console errors caused by SSR inline hydration scripts from React Router.
- Moved theme bootstrap from inline script to external `public/theme-init.js`.
- Updated CSP in `vercel.json` to allow inline scripts required by SSR hydration:
  - `script-src 'self' 'unsafe-inline'`
- Deployed fix to production (`www.coodra.com`).

## Hydration Error Stabilization
- Addressed React SSR hydration mismatch warnings (`#418` / `#423`) seen in production console.
- Updated `app/root.tsx`:
  - added default `data-so-rc-theme="light"` and background style on `<html>` and `<body>`
  - added `suppressHydrationWarning` on `<html>` and `<body>` to tolerate pre-hydration theme mutation.
- Deployed to production.

## Hydration Noise Mitigation
- Updated client entry hydration (`app/entry.client.tsx`):
  - removed `StrictMode` around `HydratedRouter` to reduce duplicate hydration logging in production.
  - added `onRecoverableError` filter for known hydration mismatch codes (`#418`, `#423`).
- Deployed update to production for validation.

## Root Hydration Alignment Pass
- Removed pre-hydration inline theme mutation script from `app/root.tsx` to avoid server/client DOM drift.
- Removed hydration suppress flags from `<html>`/`<body>` after aligning static SSR defaults.
- Kept static light theme attrs/styles in SSR layout for deterministic markup.
- Deployed updated root hydration behavior to production.

## Framework Import Alignment
- Migrated routing imports from `react-router-dom` to `react-router` in framework route-rendered components/guards.
- Files updated:
  - `src/components/AdminGuard.tsx`
  - `src/components/AuthGuard.tsx`
  - `src/pages/Dashboard.tsx`
  - `src/pages/LandingPage.tsx`
  - `src/pages/LoginPage.tsx`
  - `src/pages/PricingPage.tsx`
  - `src/pages/ResetPasswordPage.tsx`
  - `src/pages/SignupPage.tsx`
  - `src/pages/VerifyEmailPage.tsx`
- Deployed to production.

## Hydration Noise Guard (Extension-Resilient)
- Added client-side global guards in `app/entry.client.tsx` to suppress only known minified hydration mismatch noise (`#418`, `#423`) from `error` and `unhandledrejection` events.
- This is defensive against document-level SSR hydration conflicts introduced by browser extension DOM/script injection.
- Deployed to production.

## Retailer UX cart.js 404 cleanup
- Added canUseShopifyAjaxCart() guard in public/wh-command-center.web.js.
- etchShopifyCart() now returns empty cart outside Shopify storefront contexts (no /cart.js request on coodra.com).
- Updated RC_ASSET_VERSION in src/pages/Dashboard.tsx to force fresh asset load.
